apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: wangamy-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: wangamy
            port: 
              number: 80
